package timesheeters.timesheet

import io.kotest.core.spec.style.DescribeSpec
import io.kotest.matchers.shouldBe

class MyTests : DescribeSpec({
    describe("score") {
        it("start as zero") {
            val sum = 10 + 10
            sum shouldBe 15
        }

        it("start as zero") {
            val sum = 10 + 10
            sum shouldBe 15
        }
    }
})
